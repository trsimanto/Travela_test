import 'package:flutter/material.dart';

import '../../../../core/widget/custom_widget.dart';

class BeAHostScreen extends StatelessWidget {
  const BeAHostScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return CustomScaffold(body: const Center(child: Text("Be A Host"),));
  }
}
